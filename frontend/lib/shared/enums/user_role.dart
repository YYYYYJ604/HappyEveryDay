enum UserRole { user, admin, mentor }

enum PlanStatus { active, paused, completed, abandoned, archived }

enum MoodType { happy, sad, anxious, calm, angry, excited, tired, neutral, lonely, grateful }
enum MoodLevel { low, fair, good, happy }
